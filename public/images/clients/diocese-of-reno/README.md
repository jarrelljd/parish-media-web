Drop a photo here for the "Diocese of Reno" result card on `/testimonials`
(`src/components/Testimonials.tsx`).

Either works:
- A photo of the diocese/cathedral, or
- A photo of the bishop

Name it:

    photo.jpg

(or `.png` — either is fine, just tell me which you used).

Specs that'll look best in that card:
- Landscape orientation (roughly 3:2 or 16:9), not a tight square crop
- At least 1200px on the long edge so it doesn't look soft
- JPG/PNG, no huge file size needed — this isn't print

Once it's here, let me know and I'll wire it into the card. A logo/seal works too if you'd rather use that instead of or alongside a photo — just say so.
